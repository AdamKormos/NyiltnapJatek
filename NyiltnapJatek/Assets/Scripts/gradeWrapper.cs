using UnityEngine;

public class gradeWrapper : MonoBehaviour
{
    Grade[] gradeArr = new Grade[3];
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        foreach (Grade i in gradeArr)
        {
            
        }
    }
}
